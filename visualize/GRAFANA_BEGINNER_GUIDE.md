# 🎓 Grafana 처음 사용자를 위한 완벽 가이드

> **목표**: Grafana를 한 번도 안 써본 사람도 30분 안에 대시보드를 만들 수 있게!

---

## 📚 목차

1. [Grafana가 뭔가요?](#grafana가-뭔가요)
2. [설치부터 시작까지 (10분)](#설치부터-시작까지)
3. [첫 번째 패널 만들기 (5분)](#첫-번째-패널-만들기)
4. [전체 대시보드 완성하기 (15분)](#전체-대시보드-완성하기)
5. [자주 묻는 질문 & 문제 해결](#자주-묻는-질문)

---

## 🤔 Grafana가 뭔가요?

**Grafana**는 데이터를 예쁜 그래프와 차트로 보여주는 도구입니다.

### 우리가 할 일:
```
CSV 파일 (숫자 데이터)
     ↓
  Grafana
     ↓
예쁜 그래프 🎨
```

### 왜 Grafana를 쓰나요?
- ✅ 엑셀보다 훨씬 예쁨
- ✅ 실시간 업데이트 가능
- ✅ 여러 데이터를 한 화면에 정리
- ✅ 무료!

---

## 🚀 설치부터 시작까지 (10분)

### 1단계: 준비물 확인

터미널을 열고 확인하세요:

```bash
# 1. Python이 설치되어 있나요?
python3 --version
# 결과: Python 3.x.x 나오면 OK

# 2. 데이터 파일이 있나요?
cd /Users/gimhyejin/Library/CloudStorage/OneDrive-한성대학교/문서/Projects/singapore-prestolabs/BE
ls output/final/grafana_*.csv
# 결과: grafana_XXX.csv 파일들이 보이면 OK
```

만약 데이터 파일이 없다면:
```bash
./run_detection_and_generate_dashboard_data.sh
```

---

### 2단계: Grafana 설치 (3분)

**방법 A: Homebrew 사용 (추천)**

```bash
# Grafana 설치
brew install grafana

# 설치 확인
grafana-server --version
```

**방법 B: Docker 사용**

```bash
docker run -d -p 3000:3000 --name=grafana grafana/grafana
```

---

### 3단계: Grafana 실행 (1분)

```bash
# Homebrew로 설치했다면
brew services start grafana

# Docker로 설치했다면 (이미 실행 중)
docker ps | grep grafana
```

**확인 방법**:
- 웹 브라우저를 열고 http://localhost:3000 접속
- Grafana 로그인 화면이 보이면 성공!

---

### 4단계: 첫 로그인 (1분)

화면에 보이는 것:
```
┌─────────────────────────┐
│   Welcome to Grafana    │
│                         │
│  Email or username      │
│  [        admin       ] │
│                         │
│  Password               │
│  [        admin       ] │
│                         │
│      [  Log in  ]       │
└─────────────────────────┘
```

**입력하기**:
1. Email or username: `admin`
2. Password: `admin`
3. **Log in** 버튼 클릭

**비밀번호 변경 요구가 나오면**:
- Skip 클릭 (나중에 바꿀 수 있음)
- 또는 새 비밀번호 입력 (기억하기!)

---

### 5단계: CSV 파일을 웹에서 볼 수 있게 만들기 (2분)

**새 터미널 창을 열고** 실행:

```bash
cd /Users/gimhyejin/Library/CloudStorage/OneDrive-한성대학교/문서/Projects/singapore-prestolabs/BE/output/final

python3 -m http.server 8080
```

**결과**:
```
Serving HTTP on 0.0.0.0 port 8080 (http://0.0.0.0:8080/) ...
```

⚠️ **이 터미널 창은 닫지 마세요!** (Grafana가 계속 파일을 읽을 수 있게)

**확인 방법**:
- 웹 브라우저에서 http://localhost:8080 접속
- grafana_summary_stats.csv 파일이 보이면 성공!

---

### 6단계: Infinity 플러그인 설치 (3분)

**새 터미널 창을 하나 더 열고** 실행:

```bash
# 플러그인 설치
grafana-cli plugins install yesoreyeram-infinity-datasource

# Grafana 재시작
brew services restart grafana
```

**Docker 사용자라면**:
```bash
docker exec -it grafana grafana-cli plugins install yesoreyeram-infinity-datasource
docker restart grafana
```

1~2분 기다린 후 http://localhost:3000 새로고침

---

## 🎨 첫 번째 패널 만들기 (5분)

### 준비: 데이터 소스 추가하기

**화면 왼쪽에 보이는 메뉴**:
```
☰ (햄버거 아이콘)
🏠 Home
📊 Dashboards
🔍 Explore
⚙️ Configuration  ← 여기 클릭!
```

**⚙️ Configuration 클릭 후**:
1. **Data sources** 클릭
2. **Add data source** 버튼 클릭 (파란색 버튼)

**검색창에 "Infinity" 입력**:
- Infinity 플러그인이 보임
- **Select** 버튼 클릭

**설정 화면**:
```
Name: Detection CSV Data  ← 여기에 이름 입력
```

다른 건 건드리지 말고:
- 맨 아래 **Save & test** 버튼 클릭
- "Data source is working" 메시지가 나오면 성공!

---

### 첫 번째 패널: 계정 테이블 만들기

**1. 대시보드 만들기**

왼쪽 메뉴:
```
☰
📊 Dashboards  ← 클릭
```

- **New** 버튼 클릭
- **New Dashboard** 선택

**2. 패널 추가하기**

빈 화면에 큰 버튼이 보입니다:
```
┌─────────────────────────────────┐
│                                 │
│     + Add visualization         │  ← 클릭!
│                                 │
└─────────────────────────────────┘
```

**3. 데이터 소스 선택**

팝업 창에서:
```
Select data source
- -- Grafana --
- TestData DB
- Detection CSV Data  ← 이거 선택!
```

**4. 쿼리 설정하기**

화면 아래 "Query" 부분이 보입니다:

```
┌─ Query ─────────────────────────────────┐
│ Type:    [Inline ▼]                     │  ← 여기 클릭해서
│                                          │
│ Parser:  [CSV ▼]                        │  ← CSV 선택
│                                          │
│ Data:    [                            ] │  ← 여기는 건드리지 말고
└─────────────────────────────────────────┘
```

**Type을 "URL"로 바꾸기**:
1. Type 드롭다운 클릭
2. **URL** 선택

**URL 입력하기**:
```
URL: http://localhost:8080/grafana_accounts_table.csv
```

**5. 테이블로 보기**

화면 오른쪽 위:
```
Visualization: [Table ▼]  ← Table 선택
```

**6. 저장하기**

오른쪽 위 **Apply** 버튼 클릭 (파란색)

**축하합니다! 🎉 첫 번째 패널 완성!**

테이블에 계정 정보가 보입니다:
- account_id
- final_risk
- pattern1_risk, pattern2_risk, pattern3_risk
- 등등...

---

## 📊 전체 대시보드 완성하기 (15분)

### 대시보드 저장하기 (먼저!)

오른쪽 위 💾 아이콘 클릭:
```
Dashboard name: 이상 거래 탐지 대시보드
Folder: General
```

**Save** 버튼 클릭

---

### 패널 2: 리스크 요약 통계 (숫자 카드)

**1. 새 패널 추가**

오른쪽 위 **Add** 버튼 → **Visualization** 클릭

**2. 데이터 소스 선택**

`Detection CSV Data` 선택

**3. 쿼리 설정**

```
Type: URL
URL: http://localhost:8080/grafana_summary_stats.csv
Parser: CSV
```

**4. Visualization 선택**

```
Visualization: Stat  ← 숫자 카드 형태
```

**5. 값 선택**

화면 오른쪽:
```
┌─ Panel options ─────────────┐
│ Title: 총 계정 수            │
└─────────────────────────────┘

┌─ Value options ─────────────┐
│ Fields: total_accounts       │  ← 선택
└─────────────────────────────┘
```

**6. 색상 설정 (선택사항)**

```
┌─ Standard options ──────────┐
│ Color scheme: Green-Yellow-Red  │
└─────────────────────────────┘
```

**Apply** 클릭

**같은 방법으로 4개 더 만들기**:

| Title | Field | 색상 |
|-------|-------|------|
| CRITICAL | critical_count | 빨강 |
| HIGH | high_count | 주황 |
| SUSPICIOUS | suspicious_count | 노랑 |
| NORMAL | normal_count | 녹색 |

---

### 패널 3: 시계열 그래프 (추세 보기)

**1. 새 패널 추가**

Add → Visualization

**2. 쿼리**

```
Type: URL
URL: http://localhost:8080/grafana_timeseries.csv
Parser: CSV
```

**3. Visualization**

```
Visualization: Time series  ← 꺾은선 그래프
```

**4. 시간축 설정**

화면 아래 "Transform" 탭 클릭:
```
┌─ Transformations ───────────┐
│ + Add transformation        │  ← 클릭
│                             │
│ Convert field type          │  ← 선택
└─────────────────────────────┘
```

설정:
```
Field: timestamp
As: Time
```

**5. 그래프 색상 설정**

오른쪽 패널:
```
┌─ Graph styles ──────────────┐
│ critical_count → 빨강       │
│ high_count → 주황           │
│ suspicious_count → 노랑     │
└─────────────────────────────┘
```

클릭해서 색상 변경 가능

**Apply** 클릭

---

### 패널 4: 파이 차트 (리스크 비율)

**1. 새 패널 추가**

**2. 쿼리**

```
URL: http://localhost:8080/grafana_summary_stats.csv
```

**3. Visualization**

```
Visualization: Pie chart  ← 원형 차트
```

**4. 값 선택**

오른쪽 패널에서:
```
┌─ Pie chart ─────────────────┐
│ Values: All values          │
│ Labels: Field names         │
└─────────────────────────────┘
```

**5. 필요 없는 필드 숨기기**

아래 "Transform" 탭:
```
+ Add transformation
→ Filter fields by name
```

**선택할 필드만 체크**:
- ✅ critical_count
- ✅ high_count
- ✅ suspicious_count
- ✅ normal_count
- ❌ total_accounts (체크 해제)
- ❌ pattern1_high (체크 해제)
- 등등...

**Apply** 클릭

---

### 패널 5: 고위험 알림 테이블

**1. 새 패널 추가**

**2. 쿼리**

```
URL: http://localhost:8080/grafana_alerts_priority.csv
```

**3. Visualization**

```
Visualization: Table
```

**4. 색상 규칙 추가**

오른쪽 패널 → **Overrides** 탭:

```
+ Add field override
→ Fields with name: final_risk
```

**Add override property** 클릭:
```
→ Cell display mode → Color text
```

다시 **Add override property**:
```
→ Value mappings
```

**Value mappings 설정**:
```
CRITICAL → 빨강
HIGH → 주황
SUSPICIOUS → 노랑
NORMAL → 녹색
```

**Apply** 클릭

---

### 레이아웃 정리하기

**패널 크기 조절**:
1. 패널 위에 마우스 올리기
2. 모서리에 작은 점이 보임
3. 드래그해서 크기 조절

**패널 이동**:
1. 패널 제목 부분 클릭
2. 드래그해서 원하는 위치로

**추천 레이아웃**:
```
┌────────────────────────────────────────────┐
│         이상 거래 탐지 대시보드             │
└────────────────────────────────────────────┘

┌──────┬──────┬──────┬──────┬──────┐
│ 총계 │CRITICAL│ HIGH │SUSPICIOUS│NORMAL│
│  63  │   6   │  15  │    6    │  36  │
└──────┴──────┴──────┴──────┴──────┘

┌─────────────────┬──────────────────┐
│   파이 차트     │  시계열 그래프   │
│                 │                  │
└─────────────────┴──────────────────┘

┌──────────────────────────────────┐
│     고위험 계정 테이블            │
│                                   │
└──────────────────────────────────┘
```

**저장하기**: 오른쪽 위 💾 아이콘 → **Save** 클릭

---

## 🔄 자동 새로고침 설정하기

오른쪽 위에 🔄 아이콘 클릭:

```
┌─ Refresh dashboard ─────┐
│ Off                      │
│ 5s                       │
│ 10s                      │
│ 30s                      │
│ 1m   ← 추천             │
│ 5m                       │
│ 15m                      │
└─────────────────────────┘
```

**1m** 선택하면 1분마다 자동으로 새로고침

---

## 🎯 실전 사용법

### 1. 매일 아침 확인하기

```bash
# 1. 탐지 실행
cd /Users/gimhyejin/Library/CloudStorage/OneDrive-한성대학교/문서/Projects/singapore-prestolabs/BE
./run_detection_and_generate_dashboard_data.sh

# 2. CSV 서빙 (이미 실행 중이면 스킵)
cd output/final
python3 -m http.server 8080

# 3. Grafana 접속
http://localhost:3000
```

### 2. 고위험 계정 확인하기

대시보드에서:
1. **고위험 알림 테이블** 확인
2. CRITICAL/HIGH 행을 클릭
3. account_id 복사
4. 상세 조사

### 3. 검토 완료 처리하기

```python
# 터미널에서 Python 실행
python3

>>> import pandas as pd
>>> df = pd.read_csv('output/final/integrated_detection_results.csv')
>>> df.loc[df['account_id'] == 'A_1f97e16953', 'reviewed'] = True
>>> df.to_csv('output/final/integrated_detection_results.csv', index=False)
>>> exit()

# 데이터 재생성
python visualize/create_grafana_dashboard_data.py
```

Grafana에서 새로고침 → reviewed 상태 업데이트됨

---

## ❓ 자주 묻는 질문

### Q1: "No data" 메시지가 나와요

**원인**: CSV 파일을 못 읽음

**해결**:
```bash
# 1. HTTP 서버 실행 확인
curl http://localhost:8080/grafana_summary_stats.csv
# CSV 내용이 보이면 OK

# 2. 안 보이면 서버 재시작
cd output/final
python3 -m http.server 8080
```

---

### Q2: 패널이 비어있어요

**원인**: Parser 설정 잘못됨

**해결**:
1. 패널 편집 (제목 클릭 → Edit)
2. Parser가 **CSV**로 설정되어 있는지 확인
3. URL이 정확한지 확인
4. Apply 클릭

---

### Q3: 시간축이 이상해요

**원인**: timestamp가 시간 형식으로 인식 안 됨

**해결**:
1. Transform 탭 클릭
2. Add transformation → Convert field type
3. Field: `timestamp`, As: `Time`

---

### Q4: Infinity 플러그인이 안 보여요

**해결**:
```bash
# 플러그인 재설치
grafana-cli plugins install yesoreyeram-infinity-datasource

# Grafana 재시작
brew services restart grafana

# 1분 기다린 후 새로고침
```

---

### Q5: 그래프 색상을 바꾸고 싶어요

**방법 1: 개별 색상**
1. 패널 편집
2. 오른쪽 패널 → Standard options → Color scheme
3. 원하는 색상 선택

**방법 2: 조건부 색상**
1. Overrides 탭
2. Add field override
3. Cell display mode → Color background
4. Thresholds 설정

---

### Q6: 대시보드를 공유하고 싶어요

**방법 1: 스냅샷**
1. Share 아이콘 클릭
2. Snapshot 탭
3. Publish to snapshots.raintank.io
4. URL 복사해서 공유

**방법 2: JSON 내보내기**
1. 대시보드 설정 (⚙️) → JSON Model
2. Copy to clipboard
3. 파일로 저장 → 다른 사람에게 전달

---

### Q7: 데이터를 엑셀로 내보내고 싶어요

**방법**:
1. 테이블 패널에서
2. 패널 제목 옆 ⋮ (점 3개) 클릭
3. **Inspect** → **Data** 탭
4. **Download CSV** 버튼 클릭

---

## 🎓 다음 단계

### 레벨 1: 기본 사용자 ✅
- [x] Grafana 설치
- [x] 데이터 소스 추가
- [x] 기본 패널 만들기
- [x] 대시보드 저장

### 레벨 2: 중급 사용자
- [ ] 변수 사용하기 (필터 추가)
- [ ] 알림 규칙 설정
- [ ] Slack/Email 연동
- [ ] 커스텀 쿼리 작성

### 레벨 3: 고급 사용자
- [ ] SQLite 데이터 소스 사용
- [ ] 복잡한 Transformation
- [ ] 플러그인 개발
- [ ] API 연동

---

## 📚 추가 학습 자료

### 공식 문서
- [Grafana 공식 가이드](https://grafana.com/docs/grafana/latest/)
- [Infinity 플러그인 문서](https://grafana.com/grafana/plugins/yesoreyeram-infinity-datasource/)

### 유튜브 튜토리얼
- "Grafana Tutorial for Beginners" 검색
- "Grafana CSV Data Source" 검색

### 커뮤니티
- [Grafana Community Forum](https://community.grafana.com/)
- [Reddit r/grafana](https://www.reddit.com/r/grafana/)

---

## ✅ 완료 체크리스트

설치 & 설정:
- [ ] Grafana 설치 완료
- [ ] http://localhost:3000 접속 가능
- [ ] Infinity 플러그인 설치
- [ ] CSV 서버 실행 (port 8080)
- [ ] 데이터 소스 추가

첫 대시보드:
- [ ] 계정 테이블 패널 생성
- [ ] 요약 통계 패널 생성
- [ ] 시계열 그래프 생성
- [ ] 파이 차트 생성
- [ ] 레이아웃 정리
- [ ] 대시보드 저장

고급 기능:
- [ ] 자동 새로고침 설정
- [ ] 색상 규칙 적용
- [ ] 검토 상태 업데이트 방법 숙지

---

**축하합니다! 🎉**

Grafana 대시보드를 성공적으로 만들었습니다!

궁금한 점이 있으면 언제든 질문하세요.

---

**작성일**: 2025-11-14
**대상**: Grafana 처음 사용자
**소요 시간**: 약 30분
